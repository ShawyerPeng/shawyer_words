enum StudyTaskSource { mustReview, normalReview, newWord, probeWord }

enum StudyTaskReason {
  overdue,
  lowStability,
  highLapseRisk,
  backlogProtected,
  freshWord,
  probeRecheck,
}

enum StudyBacklogLevel { none, light, medium, heavy }
